include:
  - chain-8000.yaml
services:
  test:
    build: .
    entrypoint: [ "deno" ]
    command: [ "test", "--allow-net", "test.ts" ]
    volumes:
      - ../../lib:/test/lib
      - ../../deno.json:/test/deno.json